describe('Config.js', function () {
  'use strict';

  var CONFIG;

  beforeEach(function () {
    CONFIG = window.CONFIG;
  });

  it('should return 2', function () {
    expect(1 + 1).toBe(2);
  });

});
